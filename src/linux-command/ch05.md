# 第5章：用户与权限

## 为什么你需要读这一章？

Linux是一个多用户系统。理解权限系统就像理解面向对象编程中的访问控制（public/private/protected）——它决定了谁能读、谁能写、谁能执行。不懂权限，你会频繁遇到"Permission denied"错误；懂了权限，你就能精确控制谁可以做什么。

## 5.1 Linux用户模型

```
┌────────────────────────────────────────────────────┐
│  root（超级用户，UID=0）                             │
│  ├── 系统中的"上帝"，可以做任何事                    │
│  ├── 危险！不建议日常使用                            │
│  └── 就像代码中的 root access                       │
│                                                      │
│  普通用户（UID >= 1000）                             │
│  ├── 日常使用的账户                                  │
│  ├── 只能操作自己的文件                              │
│  └── 可以通过 sudo 临时获得管理员权限                │
│                                                      │
│  系统用户（UID 1-999）                               │
│  ├── 为服务/进程创建（www-data, mysql 等）            │
│  └── 通常不能登录                                    │
└────────────────────────────────────────────────────┘
```

## 5.2 查看用户信息

```bash
whoami                    # 当前用户名
id                        # 当前用户UID、GID、所属组
id bob                    # 其他用户的信息
id root                   # uid=0(root) gid=0(root)

who                       # 当前登录用户
w                         # 登录用户及活动（更详细）

cat /etc/passwd           # 所有用户信息
# 格式: username:x:UID:GID:Description:HomeDir:Shell

awk -F: '$3 >= 1000 {print $1}' /etc/passwd   # 只看普通用户
groups alice              # 用户所属的组
cat /etc/group            # 所有组信息
```

## 5.3 sudo 的原理与配置

```bash
# 基本用法
sudo apt update            # 以管理员权限执行（输入自己的密码）
sudo -i                    # 以root身份打开shell
sudo su -                  # 同上
sudo -u bob ls /home/bob/  # 以bob身份执行命令

# 配置（必须用 visudo，会检查语法）
sudo visudo
# 格式：username ALL=(ALL:ALL) ALL
# alice ALL=(ALL:ALL) ALL              # 需要密码
# bob ALL=(ALL:ALL) NOPASSWD: ALL      # 不需要密码
# charlie ALL=(ALL) /usr/bin/apt       # 只能执行apt

# 实用命令
sudo -l                    # 查看你被允许的sudo命令
sudo -k                    # 清除密码缓存
sudo !!                    # 用sudo重复上一条命令
```

## 5.4 文件权限详解：rwx

```
-rwxr-xr-x 1 alice developers 4096 Jan 15 10:30 script.sh
││││││││││ │     │
││││││││││ │     └── 所属组
││││││││││ └──────── 所有者
│││││││││└── 其他用户-执行
││││││││└─── 其他用户-写
│││││││└──── 其他用户-读
││││││└───── 所属组-执行
│││││└────── 所属组-写
││││└─────── 所属组-读
│││└──────── 所有者-执行
││└───────── 所有者-写
│└────────── 所有者-读
└─────────── 文件类型（- 文件，d 目录，l 链接）
```

| 权限 | 字母 | 数字 | 对文件 | 对目录 |
| ------ | ------ | ------ | -------- | -------- |
| 读 | r | 4 | 查看内容 | 列出内容（ls） |
| 写 | w | 2 | 修改内容 | 创建/删除文件 |
| 执行 | x | 1 | 运行程序 | 进入目录（cd） |

**常见权限组合：**

| 数字 | 符号 | 常用场景 |
| ------ | ------ | ---------- |
| 755 | rwxr-xr-x | 脚本、目录 |
| 644 | rw-r--r-- | 普通文件 |
| 700 | rwx------ | 私密目录 |
| 600 | rw------- | SSH密钥、私密配置 |
| 777 | rwxrwxrwx | 不推荐！ |

## 5.5 chmod：修改权限

```bash
# 数字法
chmod 755 script.sh        # 所有者rwx，组r-x，其他r-x
chmod 644 config.yml       # 所有者rw-，组r--，其他r--
chmod 600 ~/.ssh/id_rsa    # SSH私钥必须是600！
chmod -R 755 /var/www/html # 递归修改

# 符号法
chmod u+x script.sh        # 给所有者加执行权限
chmod a+x script.sh        # 给所有人加执行权限（= chmod +x）
chmod o-w file.txt         # 移除其他人的写权限
chmod u=rwx,g=rx,o=rx script.sh  # 组合（= 755）
chmod -R g+w project/      # 递归

# 实用示例
chmod +x deploy.sh         # 让脚本可执行
chmod 700 ~/.ssh
chmod 600 ~/.ssh/id_rsa
chmod 644 ~/.ssh/id_rsa.pub
chmod 600 ~/.ssh/config
```

## 5.6 chown 和 chgrp

```bash
sudo chown alice file.txt              # 修改所有者
sudo chown alice:developers file.txt   # 同时修改所有者和组
sudo chown :developers file.txt        # 只修改组
sudo chown -R www-data:www-data /var/www/html   # 递归

# 常见场景：修复sudo创建的文件
sudo touch /home/alice/newfile.txt     # 属于root!
sudo chown alice:alice /home/alice/newfile.txt   # 修复

sudo chgrp developers project/         # 修改组
sudo chgrp -R developers project/      # 递归
```

## 5.7 umask：默认权限掩码

```bash
umask                  # 查看当前值（默认 0022）
# 目录默认权限 = 777 - 022 = 755
# 文件默认权限 = 666 - 022 = 644

umask 077              # 更安全的设置
# 目录 = 700，文件 = 600

# 永久修改
echo "umask 077" >> ~/.bashrc
source ~/.bashrc
```

## 5.8 特殊权限：SUID、SGID、Sticky Bit

```bash
# SUID (4) - 以文件所有者身份运行
ls -l /usr/bin/passwd
# -rwsr-xr-x （注意 s 表示SUID）
sudo chmod 4755 /path/to/program

# SGID (2) - 目录中新文件自动继承组
sudo mkdir /shared
sudo chgrp developers /shared
sudo chmod 2770 /shared

# Sticky Bit (1) - 只能删自己的文件
ls -ld /tmp
# drwxrwxrwt （注意 t 表示Sticky）
sudo chmod 1777 /shared/dir

# 查找系统中的 SUID 文件（安全检查）
find / -perm -4000 -type f 2>/dev/null
```

## 5.9 ACL：高级权限控制

```bash
sudo apt install acl

# 查看ACL
getfacl file.txt

# 给特定用户加权限
setfacl -m u:bob:rw file.txt

# 给特定组加权限
setfacl -m g:developers:rx project/

# 删除特定用户的ACL
setfacl -x u:bob file.txt

# 清除所有ACL
setfacl -b file.txt

# 递归设置
setfacl -R -m u:bob:rx project/

# 设置默认ACL（新文件自动继承）
setfacl -d -m u:bob:rx project/

# ls -l 中有 + 号表示有ACL
ls -l file.txt
# -rw-rw-r--+ 1 alice alice ... file.txt
```

## 5.10 实战场景：权限问题诊断与修复

**场景1：Web服务器403错误**

```bash
# 问题：Nginx返回403 Forbidden
ls -la /var/www/html/
# -rw------- 1 alice alice ... index.html  ← 只有alice能读

# 修复：
sudo chown -R www-data:www-data /var/www/html
sudo find /var/www/html -type d -exec chmod 755 {} \;
sudo find /var/www/html -type f -exec chmod 644 {} \;
```

**场景2：SSH密钥权限错误**

```bash
# 问题：WARNING: UNPROTECTED PRIVATE KEY FILE!
# 修复：
chmod 700 ~/.ssh
chmod 600 ~/.ssh/id_rsa
chmod 644 ~/.ssh/id_rsa.pub
chmod 600 ~/.ssh/authorized_keys
chmod 600 ~/.ssh/config
```

**场景3：团队共享目录（SGID实战）**

```bash
# 创建开发团队共享目录
sudo groupadd developers
sudo usermod -aG developers alice
sudo usermod -aG developers bob

sudo mkdir /shared/project
sudo chgrp developers /shared/project
sudo chmod 2770 /shared/project
# 2770 = SGID + rwxrwx---
# 新创建的文件自动属于developers组

ls -ld /shared/project
# drwxrws--- 2 root developers ... /shared/project
```

**场景4：保护敏感配置文件**

```bash
# 配置文件只有管理员能改，应用能读
sudo chown admin:myapp /etc/myapp/config.yml
sudo chmod 640 /etc/myapp/config.yml
# 640 = 管理员读写，应用组读，其他无权限
```

## 5.11 用户管理基础命令

```bash
# 添加用户
sudo adduser newuser              # 交互式创建（推荐）
sudo useradd -r -s /usr/sbin/nologin serviceuser  # 系统用户

# 修改用户
sudo usermod -aG docker alice     # 加入docker组
sudo usermod -s /bin/zsh alice    # 更改默认shell

# 删除用户
sudo deluser username              # 保留家目录
sudo deluser --remove-home username # 删除家目录

# 密码管理
passwd                             # 修改自己的密码
sudo passwd username               # 修改他人密码
sudo passwd -l username            # 锁定账户

# 组管理
sudo groupadd teamname             # 创建组
sudo usermod -aG groupname user    # 加入组（需要重新登录生效）
groups username                    # 查看所属组

# 审计
last                               # 查看登录记录
sudo lastb                         # 查看失败的登录
sudo grep sudo /var/log/auth.log   # 查看sudo使用记录
```

## 5.12 安全最佳实践

```bash
# 1. 检查SUID文件（可能被利用提权）
find / -perm -4000 -type f 2>/dev/null

# 2. 检查可写的系统文件
find /etc /usr /bin /sbin -writable -type f 2>/dev/null

# 3. 文件完整性校验
sha256sum important_file > checksum.sha256    # 生成校验和
sha256sum -c checksum.sha256                 # 验证

# 4. 禁止root SSH登录
# 编辑 /etc/ssh/sshd_config
# PermitRootLogin no
# sudo systemctl restart sshd

# 5. 最小权限原则
# - 不用root运行应用
# - 为每个服务创建专用用户
# - 永远不chmod 777
```

## 5.13 实用技巧与避坑指南

```bash
# Permission denied 排查流程：
whoami && id                          # 1. 看看你是谁
ls -la filename && getfacl filename   # 2. 看看文件权限
groups                                # 3. 看看你在哪些组
ls -la /path/to/parent/               # 4. 检查上层目录

# 快速修复常见权限问题
chmod 700 ~                                    # 家目录
chmod 700 ~/.ssh && chmod 600 ~/.ssh/id_rsa   # SSH密钥
sudo chown -R www-data:www-data /var/www/html  # Web目录
chmod +x script.sh                             # 脚本

# 批量设置（目录和文件分开）
find project/ -type d -exec chmod 755 {} \;
find project/ -type f -exec chmod 644 {} \;

# 避坑：sudo创建的文件属于root，需要chown修复
# 避坑：目录需要 x 权限才能 cd 进入
# 避坑：cp 不保留权限，用 cp -p 或 cp -a
# 避坑：永远不要习惯性 chmod 777！
```

## 5.14 动手练习：用户与权限

**练习1：理解权限数字**

```bash
# 快速测试：创建文件并观察默认权限
touch ~/perm_test.txt
ls -la ~/perm_test.txt
# 观察默认权限（通常是644）

# 用数字法修改权限
chmod 755 ~/perm_test.txt     # rwxr-xr-x
ls -la ~/perm_test.txt

chmod 600 ~/perm_test.txt     # rw-------
ls -la ~/perm_test.txt

chmod 000 ~/perm_test.txt     # 无任何权限
ls -la ~/perm_test.txt
cat ~/perm_test.txt           # Permission denied!

chmod 644 ~/perm_test.txt     # 恢复

# 用符号法修改
chmod u+x ~/perm_test.txt     # 给所有者加执行权限
chmod g+w ~/perm_test.txt     # 给组加写权限
chmod o-r ~/perm_test.txt     # 移除其他人的读权限
ls -la ~/perm_test.txt

rm ~/perm_test.txt
```

**练习2：目录权限探索**

```bash
# 1. 创建测试目录
mkdir ~/perm_dir
touch ~/perm_dir/secret.txt
echo "top secret" > ~/perm_dir/secret.txt

# 2. 测试目录权限
chmod 000 ~/perm_dir
cd ~/perm_dir          # Permission denied!
ls ~/perm_dir          # Permission denied!

chmod 100 ~/perm_dir   # 只有执行权限（可以cd进入）
cd ~/perm_dir          # 成功！
ls                     # Permission denied!（没有r权限不能ls）

chmod 400 ~/perm_dir   # 只有读权限
ls ~/perm_dir          # 可以列出文件
cd ~/perm_dir          # Permission denied!（没有x不能cd）

chmod 700 ~/perm_dir   # 恢复正常
cd ~/perm_dir          # 成功

# 3. 清理
cd ~
rm -rf ~/perm_dir
```

**练习3：sudo 实践**

```bash
# 1. 用sudo创建文件（观察所有权）
sudo touch ~/root_file.txt
ls -la ~/root_file.txt
# -rw-r--r-- 1 root root ... ~/root_file.txt
# 文件属于root！

# 2. 尝试编辑（应该失败）
echo "hello" > ~/root_file.txt
# Permission denied!

# 3. 修复所有权
sudo chown $(whoami):$(whoami) ~/root_file.txt
ls -la ~/root_file.txt

# 4. 现在可以编辑了
echo "hello" > ~/root_file.txt
cat ~/root_file.txt

# 5. 清理
rm ~/root_file.txt
```

**练习4：权限计算速查**

```bash
# 不看表，自己计算权限数字
# rwx = 4+2+1 = 7
# rw- = 4+2+0 = 6
# r-x = 4+0+1 = 5
# r-- = 4+0+0 = 4
# --x = 0+0+1 = 1
# --- = 0+0+0 = 0

# 练习：这些权限的数字表示是什么？
# rw-rw-r-- = ?    答案：664
# rwxr-x--- = ?    答案：750
# rw------- = ?    答案：600
# r-xr-xr-x = ?    答案：555

# 验证你的答案
chmod 664 ~/test_file 2>/dev/null; ls -la ~/test_file 2>/dev/null
chmod 750 ~/test_dir 2>/dev/null; ls -ld ~/test_dir 2>/dev/null
```

## 本章小结

1. **用户模型**：root（上帝）、普通用户（日常）、系统用户（服务）
2. **sudo**：临时借用管理员权限
3. **rwx权限**：读(4)、写(2)、执行(1)，三组（所有者、组、其他）
4. **chmod**：数字法（755/644）和符号法（u+x, g-w）
5. **chown/chgrp**：修改文件的所有者和组
6. **umask**：控制新文件的默认权限
7. **特殊权限**：SUID、SGID、Sticky Bit
8. **ACL**：更精细的权限控制

## 本章命令速查表

| 命令 | 作用 | 常用示例 |
| ------ | ------ | ---------- |
| `whoami` | 当前用户名 | `whoami` |
| `id` | 用户ID和组信息 | `id` / `id username` |
| `who` | 当前登录用户 | `who` |
| `w` | 登录用户及活动 | `w` |
| `groups` | 查看用户所属组 | `groups` / `groups alice` |
| `sudo` | 以管理员权限执行 | `sudo apt update` / `sudo -i` |
| `visudo` | 编辑sudoers | `sudo visudo` |
| `chmod` | 修改权限 | `chmod 755 file` / `chmod +x script` |
| `chown` | 修改所有者 | `sudo chown user:group file` |
| `chgrp` | 修改所属组 | `sudo chgrp group file` |
| `umask` | 默认权限掩码 | `umask` / `umask 077` |
| `getfacl` | 查看ACL | `getfacl file` |
| `setfacl` | 设置ACL | `setfacl -m u:bob:rw file` |

---
