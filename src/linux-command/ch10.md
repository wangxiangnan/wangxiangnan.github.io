# 第10章 用户与组管理

## 10.1 为什么要学用户管理？

你可以把 Linux 用户系统理解为公司的组织架构：
- **用户** = 员工（每个人有自己的账号）
- **组** = 部门（一组人共享某些权限）
- **root** = CEO（拥有所有权限）
- **UID 0** = root 的"身份证号码"

## 10.2 用户信息文件

### /etc/passwd

```bash
# 查看用户信息文件
cat /etc/passwd
# 格式：用户名:密码占位:UID:GID:GECOS:家目录:Shell

# 查看所有普通用户
awk -F: '$3 >= 1000 {print $1, $3, $6, $7}' /etc/passwd

# 查看某个用户的信息
getent passwd alice
id alice
whoami
```

### /etc/shadow

```bash
# 查看密码信息（需要 root 权限）
sudo cat /etc/shadow | head -5
# 格式：用户名:密码哈希:最后修改天数:最小间隔:最大间隔:警告期:不活跃期:过期日期
# $6$ = SHA-512，* 或 ! = 账号被锁定
```

### /etc/group

```bash
# 查看组信息
cat /etc/group
# 格式：组名:密码:GID:成员列表

# 查看某个用户属于哪些组
groups alice

# 查看某个组有哪些成员
getent group docker
```

## 10.3 useradd——创建用户

```bash
# 推荐的创建方式
sudo useradd -m -s /bin/bash -G sudo,docker -c "Bob Johnson" bob
# -m 创建家目录  -s 设置Shell  -G 附加组  -c 描述

# 创建系统用户（不能登录）
sudo useradd -r -s /usr/sbin/nologin myservice

# 设置密码
sudo passwd bob

# 查看创建结果
id bob
```

## 10.4 usermod——修改用户属性

```bash
# 将用户加入组（-aG 是最常用的选项组合！）
sudo usermod -aG docker alice
# -a = append，一定要用！否则替换掉所有附加组

# 修改用户的默认 Shell
sudo usermod -s /bin/zsh alice

# 锁定用户账号
sudo usermod -L bob

# 解锁用户账号
sudo usermod -U bob

# 设置账号过期日期
sudo usermod -e 2025-12-31 tempuser
```

## 10.5 userdel——删除用户

```bash
# 删除用户及其家目录
sudo userdel -r bob

# 删除前先终止用户进程
sudo killall -u bob
sudo userdel -r bob
```

## 10.6 passwd——设置和修改密码

```bash
# 修改当前用户的密码
passwd

# root 修改其他用户的密码
sudo passwd alice

# 强制用户下次登录时修改密码
sudo passwd -e alice

# 锁定密码（禁止密码登录）
sudo passwd -l alice

# 设置密码过期策略
sudo passwd -n 7 -x 90 -w 7 alice
```

## 10.7 组管理

```bash
# 创建新组
sudo groupadd developers

# 修改组名
sudo groupmod -n engineering developers

# 删除组
sudo groupdel devops

# 将用户加入组（推荐方式）
sudo usermod -aG developers alice

# 临时切换组
newgrp developers

# 查看组的所有成员
getent group developers
```

## 10.8 su 与 sudo——切换用户

```bash
# 切换到 root
su -

# 切换到指定用户
su - alice

# 以 root 身份执行命令
sudo ls -la /root

# 以指定用户身份执行命令
sudo -u alice ls -la /home/alice

# 切换到 root shell
sudo -i

# 查看 sudo 权限
sudo -l

# 编辑 sudoers（永远用 visudo！）
sudo visudo
# 配置格式：user HOST=(run_as) command
# alice ALL=(ALL:ALL) ALL
# %developers ALL=(ALL) ALL
```

## 10.9 用户环境

```bash
# 加载顺序：
# 登录Shell: /etc/profile → ~/.profile → ~/.bash_profile
# 非登录Shell: ~/.bashrc

# 给所有用户添加环境变量
echo 'export JAVA_HOME=/usr/lib/jvm/java-17' | sudo tee /etc/profile.d/java.sh

# 给单个用户添加
echo 'export EDITOR=vim' >> ~/.bashrc

# 使配置立即生效
source ~/.bashrc
```

## 10.10 实战：为开发团队创建用户和权限体系

```bash
# 1. 创建组
sudo groupadd developers
sudo groupadd devops

# 2. 创建用户
sudo useradd -m -s /bin/bash -G developers -c "Alice Wang" alice
sudo useradd -m -s /bin/bash -G developers,devops -c "Charlie Zhang" charlie

# 3. 配置 sudo
sudo visudo -f /etc/sudoers.d/devteam
# %devops ALL=(ALL) NOPASSWD: /bin/systemctl *

# 4. 创建共享项目目录（setgid）
sudo mkdir -p /opt/projects
sudo chown root:developers /opt/projects
sudo chmod 2775 /opt/projects
# 2=setgid（新文件继承目录的组） 77=rwxrwx 5=r-x
```

## 10.11 本章小结

记住：
- **永远不要用 root 日常操作**
- **usermod -aG 中的 -a 不能漏**
- **visudo 而不是直接编辑**

## 10.12 命令速查表

| 命令 | 用途 | 常用示例 |
|------|------|----------|
| `useradd` | 创建用户 | `sudo useradd -m -s /bin/bash -G group user` |
| `usermod` | 修改用户 | `sudo usermod -aG docker alice` |
| `userdel` | 删除用户 | `sudo userdel -r user` |
| `passwd` | 设置密码 | `sudo passwd user` |
| `groupadd` | 创建组 | `sudo groupadd developers` |
| `groupdel` | 删除组 | `sudo groupdel groupname` |
| `id` | 查看用户信息 | `id user` |
| `su` | 切换用户 | `su - alice` |
| `sudo` | 以其他用户执行命令 | `sudo -u alice cmd` |
| `visudo` | 编辑 sudoers | `sudo visudo` |

---
