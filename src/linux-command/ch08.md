# 第8章 软件包管理

## 8.1 为什么要学包管理？

在 Windows 上装软件，你下载一个 `.exe`，双击"下一步、下一步"。在 Linux 上呢？我们有**包管理器**——一个命令行版的"应用商店"，只不过它管理的不是 App，而是精确到每一个共享库文件的**软件包（Package）**。

如果你把操作系统比作一套乐高积木，那包管理器就是帮你精确管理每一块积木的工具——知道每块积木装在哪里、缺了什么、哪些积木互相依赖。

## 8.2 Debian 包管理系统概览

Debian/Ubuntu 生态有两层包管理工具：

- **dpkg**：底层包管理工具，只负责安装、卸载 `.deb` 文件，不处理依赖关系。就像手动安装 npm 包——你得自己确保依赖都在。
- **apt**：高层包管理工具，自动从远程仓库下载包并解决依赖关系。就像 `npm install`——帮你把依赖树搞定。

## 8.3 apt 命令全家福

### 8.3.1 基础操作

```bash
# 更新包索引（同步远程仓库的最新信息）
sudo apt update
# 这不会安装任何东西，只是更新"有哪些包可用"的数据库

# 升级所有已安装的包
sudo apt upgrade

# 升级所有包（包括需要安装新依赖的）
sudo apt full-upgrade

# 安装一个包
sudo apt install nginx

# 安装多个包
sudo apt install nginx curl git

# 安装特定版本
sudo apt install nginx=1.18.0-0ubuntu1.4

# 卸载一个包（保留配置文件）
sudo apt remove nginx

# 卸载一个包（连配置文件一起删掉）
sudo apt purge nginx

# 自动删除不再需要的依赖包
sudo apt autoremove
```

### 8.3.2 搜索与信息查看

```bash
# 搜索包名
apt search web server

# 查看包的详细信息
apt show nginx

# 查看包的依赖关系
apt depends nginx

# 查看哪些包依赖于某个包
apt rdepends nginx

# 列出已安装的包
apt list --installed

# 列出可升级的包
apt list --upgradable

# 查看包的可用版本
apt policy nginx
```

### 8.3.3 apt 高级技巧

```bash
# 模拟安装（不真正安装）
sudo apt install --dry-run nginx

# 不安装推荐的包（最小安装）
sudo apt install --no-install-recommends nginx

# 锁定包版本（防止被升级）
sudo apt-mark hold nginx

# 解除锁定
sudo apt-mark unhold nginx

# 查看被锁定的包
apt-mark showhold

# 清理下载的包缓存
sudo apt clean
sudo apt autoclean
```

### 8.3.4 apt pinning（包固定策略）

```bash
# 创建 pinning 配置
sudo tee /etc/apt/preferences.d/nginx-pin <<'EOF'
Package: nginx
Pin: release a=jammy-security
Pin-Priority: 900
EOF

# Pin-Priority 含义：
# > 1000：即使降级也要安装这个版本
# 990：优先安装  500：正常  100：低优先级  < 0：永远不安装
```

## 8.4 dpkg 底层操作

```bash
# 查看系统中所有已安装的包
dpkg -l | head -20

# 搜索已安装的包
dpkg -l | grep nginx

# 查看某个包的详细信息
dpkg -s nginx

# 列出一个包安装的所有文件
dpkg -L nginx

# 查看某个文件属于哪个包
dpkg -S /etc/nginx/nginx.conf

# 安装一个 .deb 文件
sudo dpkg -i package.deb

# 修复依赖问题
sudo apt install -f

# 重新配置一个已安装的包
sudo dpkg-reconfigure tzdata

# 配置所有未配置完成的包
sudo dpkg --configure -a
```

## 8.5 .deb 包手动安装与依赖解决

```bash
# 第一步：安装
sudo dpkg -i google-chrome-stable_current_amd64.deb

# 第二步：修复依赖
sudo apt install -f

# 更好的做法：一步到位（apt 直接处理 .deb 文件）
sudo apt install ./google-chrome-stable_current_amd64.deb
# 注意：必须用 ./ 前缀指定本地文件路径
```

## 8.6 PPA（Personal Package Archive）

```bash
# 安装工具
sudo apt install software-properties-common

# 添加一个 PPA
sudo add-apt-repository ppa:ondrej/php

# 安装 PPA 中的包
sudo apt install php8.3

# 查看已添加的 PPA
ls /etc/apt/sources.list.d/

# 删除一个 PPA
sudo add-apt-repository --remove ppa:ondrej/php

# 或使用 ppa-purge（更安全，会降级回官方版本）
sudo apt install ppa-purge
sudo ppa-purge ppa:ondrej/php
```

## 8.7 snap 包管理

```bash
# 安装 snap 包
sudo snap install vscode --classic
sudo snap install firefox

# 查看已安装的 snap 包
snap list

# 搜索 snap 包
snap find editor

# 查看 snap 包信息
snap info vscode

# 更新 snap 包
sudo snap refresh vscode

# 回滚到上一个版本
sudo snap revert vscode

# 卸载 snap 包
sudo snap remove vscode
```

## 8.8 Flatpak 简介

```bash
# 安装 Flatpak
sudo apt install flatpak

# 添加 Flathub 仓库
flatpak remote-add --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo

# 搜索应用
flatpak search gimp

# 安装应用
flatpak install flathub org.gimp.GIMP

# 运行应用
flatpak run org.gimp.GIMP

# 列出已安装的应用
flatpak list

# 更新应用
flatpak update

# 卸载应用
flatpak uninstall org.gimp.GIMP
```

## 8.9 源码编译安装

```bash
# 安装编译工具链
sudo apt install build-essential

# 下载源码
wget https://example.com/software-1.0.tar.gz
tar -xzf software-1.0.tar.gz
cd software-1.0

# 配置（检查依赖、生成 Makefile）
./configure --prefix=/usr/local

# 编译（多核加速）
make -j$(nproc)

# 安装
sudo make install

# 更新动态链接库缓存
sudo ldconfig

# 卸载
sudo make uninstall
```

## 8.10 安装方式对比表

| 特性 | apt | snap | flatpak | 源码编译 |
| ------ | ----- | ------ | --------- | ---------- |
| 依赖处理 | 自动 | 自包含 | 自包含 | 手动 |
| 更新管理 | apt upgrade | 自动更新 | flatpak update | 手动重新编译 |
| 系统隔离 | 否 | 部分 | 完全 | 否 |
| 包大小 | 小 | 大 | 大 | N/A |
| 安全沙盒 | 否 | 是 | 是 | 否 |
| 自定义编译 | 否 | 否 | 否 | 是 |
| 适用场景 | 系统工具、服务 | 桌面应用 | 桌面应用 | 特殊需求 |
| 推荐度 | 首选 | 备选 | 备选 | 最后手段 |

## 8.11 本章小结

包管理优先级：apt > PPA > snap/flatpak > 源码编译。

```bash
# 日常维护三板斧
sudo apt update              # 1. 更新索引
sudo apt upgrade             # 2. 升级软件
sudo apt autoremove          # 3. 清理孤儿包
```

## 8.12 命令速查表

| 命令 | 用途 | 常用示例 |
| ------ | ------ | ---------- |
| `apt update` | 更新包索引 | `sudo apt update` |
| `apt upgrade` | 升级所有包 | `sudo apt upgrade` |
| `apt install` | 安装软件包 | `sudo apt install nginx` |
| `apt remove` | 卸载软件包 | `sudo apt remove nginx` |
| `apt purge` | 卸载包和配置 | `sudo apt purge nginx` |
| `apt autoremove` | 清理孤儿依赖 | `sudo apt autoremove` |
| `apt search` | 搜索软件包 | `apt search web server` |
| `apt show` | 查看包信息 | `apt show nginx` |
| `apt-mark hold` | 锁定包版本 | `sudo apt-mark hold nginx` |
| `dpkg -i` | 安装 .deb 包 | `sudo dpkg -i pkg.deb` |
| `dpkg -l` | 列出已安装包 | `dpkg -l \| grep nginx` |
| `dpkg -L` | 查看包文件列表 | `dpkg -L nginx` |
| `dpkg -S` | 查找文件所属包 | `dpkg -S /usr/bin/nginx` |

---
