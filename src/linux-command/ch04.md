# 第4章：查看与编辑文件

## 为什么你需要读这一章？

作为开发者，你每天都在和文件打交道：查看日志、阅读配置文件、编辑代码、比较文件差异。这一章将教你用各种工具高效地查看和编辑文件——从简单的`cat`到强大的`vim`，从查看文件头尾到实时追踪日志。

## 4.1 cat：查看与拼接文件

`cat`（Concatenate）把文件内容一股脑地倒出来。

```bash
# 查看文件内容
cat /etc/hostname
# 输出: my-laptop

cat /etc/shells
# 输出所有可用的shell

# 显示行号
cat -n /etc/hosts

# 压缩连续空行
cat -s messy_file.txt

# 拼接多个文件
cat file1.txt file2.txt file3.txt
cat file1.txt file2.txt > combined.txt

# 配合 here document 快速创建文件
cat > newfile.txt << EOF
这是第一行
这是第二行
这是第三行
EOF

# 追加内容
cat >> logfile.txt << EOF
[$(date)] 新的日志条目
EOF
```

## 4.2 less：分页浏览

```bash
# 基本用法
less /var/log/syslog

# 快捷键：
# q          退出
# Space / f  下一页
# b          上一页
# g          跳到文件开头
# G          跳到文件末尾
# 50g        跳到第50行
# /keyword   向下搜索
# ?keyword   向上搜索
# n / N      下一个/上一个搜索结果
# v          用编辑器打开当前文件

# 常用选项
less -N /var/log/syslog     # 显示行号
less -S long_file.txt        # 不自动换行
less -I /var/log/syslog      # 搜索忽略大小写

# 管道方式使用
ps aux | less
docker logs mycontainer | less
```

## 4.3 head 和 tail

```bash
# head - 查看文件开头（默认前10行）
head /var/log/syslog
head -20 /var/log/syslog
head -n 5 config.yml
head -c 100 binary_file     # 前100字节

# tail - 查看文件末尾（默认后10行）
tail /var/log/syslog
tail -20 /var/log/syslog
tail -n 5 config.yml

# tail -f —— 实时追踪（排查问题神器！）
tail -f /var/log/nginx/access.log
tail -f /var/log/syslog | grep "ERROR"
tail -50f /var/log/syslog       # 最后50行+实时追踪

# tail -F —— 更强大（跟踪文件名，支持日志轮转）
tail -F /var/log/app.log

# 组合技
head -30 file.txt | tail -11     # 查看第20-30行
tail -n +6 file.txt              # 跳过前5行
head -n -3 file.txt              # 去掉最后3行
```

## 4.4 wc：统计文件信息

```bash
wc report.txt          # 行数 单词数 字节数 文件名
wc -l report.txt       # 只显示行数
wc -w report.txt       # 只显示单词数
wc -c report.txt       # 只显示字节数

# 实用组合
find . -name "*.py" | wc -l           # Python文件数量
wc -l src/*.py                         # 每个文件的行数
wc -l *.py | sort -rn | head -5       # 行数最多的5个文件
wc -l /etc/passwd                      # 系统用户数量
```

## 4.5 diff：比较文件差异

```bash
# 基本用法
diff file1.txt file2.txt

# diff -u —— 统一格式（类似 Git diff）
diff -u file1.txt file2.txt

diff -i file1.txt file2.txt     # 忽略大小写
diff -w file1.txt file2.txt     # 忽略空白差异
diff -q file1.txt file2.txt     # 只报告是否不同
diff -r dir1/ dir2/             # 递归比较目录

# colordiff（推荐安装）
sudo apt install colordiff
colordiff file1.txt file2.txt   # 彩色输出
alias diff='colordiff'

# diff + patch
diff -u original.txt modified.txt > fix.patch
patch original.txt < fix.patch
```

## 4.6 nano 编辑器入门

```bash
nano filename.txt              # 打开文件
nano +42 filename.txt          # 打开并跳到第42行

# 常用快捷键：
# Ctrl+O   保存
# Ctrl+X   退出
# Ctrl+R   插入文件
# Ctrl+W   搜索
# Ctrl+\   搜索并替换
# Ctrl+_   跳到指定行
# Ctrl+K   剪切整行
# Ctrl+U   粘贴
# Alt+\    跳到文件开头
# Alt+/    跳到文件末尾

# 配置文件 ~/.nanorc
# set linenumbers
# set autoindent
# set tabsize 4
# set tabstospaces
# include "/usr/share/nano/*.nanorc"
```

## 4.7 vim 编辑器入门

**vim 三种模式：**

```
普通模式（Normal）── i ──> 插入模式（Insert）
       │                        │
       │（:）                   │（Esc）
       v                        │
命令模式（Command）<────────────┘
```

```bash
sudo apt install vim
vim filename.txt
vim +42 filename.txt     # 打开并跳到第42行

# 普通模式操作：
# h/j/k/l    左/下/上/右移动
# w / b      下一个/上一个单词
# 0 / $      行首 / 行尾
# gg / G     文件开头 / 末尾
# 42G        跳到第42行
# dd         删除整行
# yy         复制整行
# p          粘贴
# u          撤销
# Ctrl+r     重做
# /keyword   搜索

# 进入插入模式：
# i  光标前插入
# a  光标后插入
# o  下方新建行
# O  上方新建行
# Esc 回到普通模式

# 命令模式（按 : 进入）：
# :w       保存
# :q       退出
# :wq      保存退出
# :q!      强制退出不保存
# :%s/old/new/g   全文替换
# :sp      水平分屏
# :vs      垂直分屏
```

```bash
# ~/.vimrc 基础配置
# set number              " 显示行号
# set relativenumber      " 相对行号
# set cursorline          " 高亮当前行
# set hlsearch            " 高亮搜索结果
# set incsearch           " 实时搜索
# set ignorecase          " 忽略大小写
# set tabstop=4           " Tab宽度
# set expandtab           " Tab转空格
# set autoindent          " 自动缩进
```

**vim 可视模式（Visual Mode）：**

```bash
# 可视模式让你用类似鼠标选择的方式操作文本
# 三种可视模式：
# v          字符级可视模式
# V          行级可视模式
# Ctrl+v     块级可视模式（列选择，超实用！）

# 可视模式操作流程：
# 1. 按 v/V/Ctrl+v 进入可视模式
# 2. 用 h/j/k/l/w/b 扩展选区
# 3. 执行操作：d删除, y复制, c修改, >/<缩进

# 块级可视模式（Ctrl+v）经典用法：批量注释
# Ctrl+v → jjjjj（选6行）→ I → 输入 "// " → Esc
# 所有选中行前面都会加上 "// "

# 取消注释：
# Ctrl+v → 选中每行开头的 "// " → d
# 一次性删除所有注释符号

# 实用文本对象操作（在普通模式下）：
# ciw     修改光标所在的单词（change inner word）
# ci"     修改引号内的内容
# ci(     修改括号内的内容
# di"     删除引号内内容（保留引号）
# di{     删除大括号内内容
# yi(     复制括号内内容
# vit     选中HTML标签内内容
# vat     选中HTML标签及其内容
```

**vim 宏（Macro）—— 录制并重复操作：**

```bash
# 宏是vim的杀手级功能——录制操作序列然后无限重复

# 录制宏：
# qa     开始录制到寄存器a
# (执行你的编辑操作)
# q      停止录制

# 播放宏：
# @a     播放寄存器a的宏
# 10@a   播放10次
# @@     重复上一次宏

# 示例：给每行添加前缀
# 1. qa → I（行首插入）→ 输入前缀 → Esc → j → q
# 2. 99@a 对剩余99行重复

# 示例：将列表项转为代码
# 假设每行是: item_name
# 目标：var item_name = require('item_name');
# qa → I → 输入 "var " → Esc → A → 输入 " = require('" → Esc
# → 0 → w → y$ → A → 输入 "');" → Esc → j → q
```

**vim 寄存器（Register）—— 26个剪贴板：**

```bash
# vim有26个命名寄存器（a-z）
"ayy     复制当前行到寄存器a
"byy     复制当前行到寄存器b
"ap      粘贴寄存器a
"bp      粘贴寄存器b

# 查看寄存器
:reg

# 特殊寄存器：
# "*  系统剪贴板（与Ctrl+C/V互通）
# "+  系统剪贴板
# "_  黑洞寄存器（丢弃）
# "0  最近一次yank

# 复制到系统剪贴板
"+yy    复制到剪贴板
"+p     从剪贴板粘贴

# 在 ~/.vimrc 中自动使用系统剪贴板
set clipboard=unnamedplus
```

**vim 标记（Mark）—— 文件中的书签：**

```bash
ma       在当前位置设置标记a
`a       跳转到标记a（精确位置）
'a       跳转到标记a（行首）
``       跳回上一次跳转前的位置
`.       跳转到最后修改的位置
:marks   查看所有标记
```

## 4.8 编辑器选择建议

| 场景 | 推荐工具 | 理由 |
| ------ | ---------- | ------ |
| 快速修改配置 | nano | 零学习成本 |
| 服务器编辑 | vim | 几乎必装 |
| 开发项目 | VS Code Remote | 图形化+插件 |
| 阅读代码库 | less / vim | 搜索快、跳转方便 |

## 4.9 实用技巧与避坑指南

```bash
# 查看大文件策略
cat config.yml                       # < 100行
less /var/log/syslog                 # 100-10000行
tail -f /var/log/nginx/access.log    # 实时追踪
grep "ERROR" /var/log/syslog         # 搜索关键词

# sort + uniq 快速分析
sort names.txt | uniq -c | sort -rn  # 统计频率

# split 分割大文件
split -l 1000 bigfile.txt chunk_     # 按行数
split -b 10M bigfile.tar.gz chunk_   # 按大小
cat chunk_* > bigfile_restored       # 合并

# 避坑：vim 中文件只读时强制保存
# :w !sudo tee %

# 避坑：tail -f 在日志轮转时可能失效
# 用 tail -F（大写F）替代
```

## 4.10 动手练习：文件查看与编辑

**练习1：文件查看工具对比**

```bash
# 创建测试文件（200行数字）
seq 1 200 > ~/numbers.txt

# 用不同方式查看
cat ~/numbers.txt | head -10      # 前10行
cat ~/numbers.txt | tail -10      # 后10行
less ~/numbers.txt                 # 分页浏览（q退出）

# 统计信息
wc -l ~/numbers.txt                # 行数
wc -w ~/numbers.txt                # 单词数

# 查看特定范围
head -50 ~/numbers.txt | tail -20  # 第31-50行

# 清理
rm ~/numbers.txt
```

**练习2：nano 编辑器实操**

```bash
nano ~/practice.conf
# 输入：
# [server]
# host = localhost
# port = 8080
# [database]
# name = myapp
# Ctrl+O保存，Ctrl+W搜索"port"，修改为3000
# Ctrl+O再次保存，Ctrl+X退出

cat ~/practice.conf     # 验证
rm ~/practice.conf      # 清理
```

**练习3：vim 基础操作**

```bash
vim ~/vim_practice.txt
# 按 i → 输入 "Hello World" → Esc
# 按 o → 输入 "Second line" → Esc
# yy → p（复制粘贴）→ u（撤销）
# :%s/World/Linux/g（全局替换）
# :wq 保存退出

cat ~/vim_practice.txt   # 验证
rm ~/vim_practice.txt    # 清理
```

**练习4：diff 文件比较**

```bash
cat > ~/file_a.txt << EOF
line one
line two
line three
line four
EOF

cat > ~/file_b.txt << EOF
line one
line TWO modified
line three
new line here
line four
EOF

diff ~/file_a.txt ~/file_b.txt           # 标准格式
diff -u ~/file_a.txt ~/file_b.txt        # 统一格式

# 生成并应用补丁
diff -u ~/file_a.txt ~/file_b.txt > ~/fix.patch
cp ~/file_a.txt ~/file_c.txt
patch ~/file_c.txt < ~/fix.patch
diff ~/file_b.txt ~/file_c.txt           # 应无差异

rm ~/file_a.txt ~/file_b.txt ~/file_c.txt ~/fix.patch
```

## 4.11 文本处理基础工具

在命令行中处理文本是开发者的日常。以下工具虽然简单，但组合使用时威力巨大。

**cut：按列提取**

```bash
# 从 /etc/passwd 中提取用户名（第1列）
cut -d: -f1 /etc/passwd
# -d:  用冒号作为分隔符
# -f1  提取第1个字段

# 提取用户名和shell（第1列和第7列）
cut -d: -f1,7 /etc/passwd

# 按字符位置提取（不需要分隔符）
echo "Hello World" | cut -c1-5
# 输出: Hello

# 提取日期中的年月日
echo "2025-01-15" | cut -d'-' -f1,2
# 输出: 2025-01
```

**tr：字符转换与替换**

```bash
# 大小写转换
echo "Hello World" | tr 'a-z' 'A-Z'
# 输出: HELLO WORLD

echo "HELLO" | tr 'A-Z' 'a-z'
# 输出: hello

# 替换字符
echo "hello world" | tr ' ' '_'
# 输出: hello_world

# 删除字符
echo "hello123world" | tr -d '0-9'
# 输出: helloworld

# 压缩重复字符
echo "hello    world" | tr -s ' '
# 输出: hello world（多个空格变为一个）

# 实用：将路径分隔符替换
echo "/home/alice/projects/webapp" | tr '/' '\n'
# 输出:
# (空行)
# home
# alice
# projects
# webapp
```

**sort：排序**

```bash
# 基本排序（字典序）
echo -e "banana\napple\ncherry" | sort
# apple
# banana
# cherry

# 数字排序
echo -e "10\n2\n30\n1" | sort -n
# 1
# 2
# 10
# 30

# 逆序（从大到小）
echo -e "10\n2\n30\n1" | sort -rn
# 30
# 10
# 2
# 1

# 按特定列排序
# 假设文件内容为：name age city
sort -k2 -n data.txt       # 按第2列数字排序
sort -k3 -r data.txt       # 按第3列逆序排序

# 排序并去重
sort -u file.txt           # -u = unique

# 人类可读的大小排序
du -sh */ | sort -rh       # -h 理解 K/M/G 单位

# 按月份排序（适用于日志）
sort -M months.txt         # 理解 Jan, Feb, Mar...

# 稳定排序（保持相等元素的原始顺序）
sort -s -k1 file.txt
```

**uniq：去重与统计**

```bash
# 注意：uniq 只去除相邻的重复行！
# 所以通常先 sort 再 uniq

echo -e "a\nb\na\nc\nb" | uniq
# a
# b
# a       ← 没有被去掉（因为不相邻）
# c
# b

# 正确做法：先排序
echo -e "a\nb\na\nc\nb" | sort | uniq
# a
# b
# c

# uniq -c 统计出现次数
echo -e "a\nb\na\nc\nb\na" | sort | uniq -c
#   3 a
#   2 b
#   1 c

# 只显示重复的行（出现超过1次）
echo -e "a\nb\na\nc\nb\na" | sort | uniq -d
# a
# b

# 只显示不重复的行（只出现1次）
echo -e "a\nb\na\nc\nb\na" | sort | uniq -u
# c

# 经典组合：统计频率并排序
cat names.txt | sort | uniq -c | sort -rn | head -10
# 找出出现最多的10个名字
```

**column：格式化表格输出**

```bash
# 将分隔符格式化为对齐的表格
echo -e "name,age,city\nAlice,30,Beijing\nBob,25,Shanghai" | column -t -s','
# name   age  city
# Alice  30   Beijing
# Bob    25   Shanghai

# 将文件内容排成多列
ls /usr/bin | column
# 多列对齐显示
```

**grep：文本搜索（基础篇，第22章详细讲）**

```bash
# 在文件中搜索关键词
grep "error" /var/log/syslog

# 递归搜索目录
grep -r "TODO" ~/projects/
# -r 递归搜索所有文件

# 显示行号
grep -n "error" file.txt
# 42:connection error occurred

# 忽略大小写
grep -i "error" file.txt

# 反向搜索（排除）
grep -v "debug" file.txt
# 显示所有不包含 "debug" 的行

# 只统计匹配的行数
grep -c "error" file.txt

# 搜索多个模式
grep -E "error|warning|critical" file.txt
# -E 启用扩展正则表达式

# 只显示匹配的部分
grep -o "[0-9]\{3\}\.[0-9]\{3\}\.[0-9]\{3\}\.[0-9]\{3\}" access.log
# 只提取IP地址

# 上下文显示
grep -B2 -A2 "error" file.txt
# -B2 显示匹配行前2行
# -A2 显示匹配行后2行
# -C2 前后各2行

# 常用组合
grep -rn "function" --include="*.js" .
# 递归搜索所有.js文件中的 "function"，显示行号

grep -rni "fixme\|todo\|hack" --include="*.py" .
# 搜索代码中的待办标记（忽略大小写）
```

## 本章小结

1. **cat**：快速查看小文件、拼接文件
2. **less**：分页浏览大文件，支持搜索和标记
3. **head/tail**：查看文件头尾，`tail -f`实时追踪日志
4. **wc**：统计行数、单词数、字节数
5. **diff**：比较文件差异，生成补丁
6. **nano**：最简单的编辑器，适合快速修改
7. **vim**：最强大的编辑器，值得投入时间学习

## 本章命令速查表

| 命令 | 作用 | 常用示例 |
| ------ | ------ | ---------- |
| `cat` | 查看/拼接文件 | `cat -n file` / `cat f1 f2 > combined` |
| `less` | 分页浏览 | `less file`（q退出，/搜索） |
| `head` | 查看文件开头 | `head -20 file` / `head -n 5 file` |
| `tail` | 查看文件末尾 | `tail -20 file` / `tail -f logfile` |
| `wc` | 统计文件 | `wc -l file` / `find . -name "*.py" \| wc -l` |
| `diff` | 比较文件 | `diff -u file1 file2` / `diff -r dir1 dir2` |
| `sort` | 排序 | `sort file` / `sort -rn file` |
| `uniq` | 去重 | `sort file \| uniq -c` |
| `split` | 分割文件 | `split -l 1000 bigfile chunk_` |
| `nano` | 简单编辑器 | `nano file`（Ctrl+O保存，Ctrl+X退出） |
| `vim` | 高级编辑器 | `vim file`（i插入，Esc，:wq保存退出） |

---
