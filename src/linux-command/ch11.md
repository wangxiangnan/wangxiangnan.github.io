# 第11章 Systemd 与服务管理

## 11.1 为什么要学 Systemd？

Systemd 是现代 Linux 的**初始化系统和服务管理器**，相当于汽车的 ECU——负责启动服务、管理依赖、记录日志、调度定时任务。不学 Systemd，你就无法理解"为什么这个服务启动不了"。

## 11.2 Systemd 架构与 Unit 类型

| Unit 类型 | 文件后缀 | 用途 |
| ----------- | ---------- | ------ |
| **Service** | .service | 管理后台服务 |
| **Socket** | .socket | 管理套接字 |
| **Timer** | .timer | 定时任务 |
| **Target** | .target | Unit 集合（类似运行级别） |
| **Mount** | .mount | 管理挂载点 |
| **Path** | .path | 监控文件变化 |

```bash
# 查看所有活跃的服务 unit
systemctl list-units --type=service

# 查看所有活跃的 timer unit
systemctl list-units --type=timer
```

## 11.3 systemctl 命令全家福

### 服务生命周期管理

```bash
# 启动/停止/重启/重载
sudo systemctl start nginx
sudo systemctl stop nginx
sudo systemctl restart nginx
sudo systemctl reload nginx          # 热更新配置（不中断服务）
sudo systemctl reload-or-restart nginx

# 查看服务状态
systemctl status nginx

# 检查是否运行/是否开机自启
systemctl is-active nginx
systemctl is-enabled nginx
```

### 开机自启管理

```bash
# 设置开机自启
sudo systemctl enable nginx

# 启用并立即启动
sudo systemctl enable --now nginx

# 取消开机自启
sudo systemctl disable nginx
```

### 查看和过滤 Unit

```bash
# 列出所有正在运行的 unit
systemctl list-units

# 只列出失败的 unit（排障利器！）
systemctl list-units --failed

# 列出所有 unit 文件
systemctl list-unit-files

# 查看 unit 文件内容
systemctl cat nginx.service

# 查看依赖关系
systemctl list-dependencies nginx.service
```

### 系统级操作

```bash
# 修改 unit 文件后必须执行！
sudo systemctl daemon-reload

# 查看/修改默认启动目标
systemctl get-default
sudo systemctl set-default multi-user.target    # 命令行模式
sudo systemctl set-default graphical.target     # 图形界面模式

# 重启/关机
sudo systemctl reboot
sudo systemctl poweroff
```

## 11.4 journalctl 日志查看

```bash
# 查看某个服务的日志
journalctl -u nginx

# 实时跟踪日志（类似 tail -f）
journalctl -u nginx -f

# 查看最近 N 行
journalctl -u nginx -n 100

# 按时间范围查看
journalctl --since "2024-01-01" --until "2024-01-02"
journalctl --since today
journalctl --since "1 hour ago"

# 按优先级查看
journalctl -p err            # 只看 error 及以上
# 优先级：0=emerg 1=alert 2=crit 3=err 4=warning 5=notice 6=info 7=debug

# 查看某次启动的日志
journalctl -b 0              # 当前启动
journalctl -b -1             # 上次启动

# 以 JSON 格式输出
journalctl -u nginx -o json

# 清理旧日志
sudo journalctl --vacuum-time=7d
sudo journalctl --vacuum-size=500M
```

## 11.5 编写自定义 Service Unit

```ini
# /etc/systemd/system/myapp.service
[Unit]
Description=My Awesome Node.js Application
After=network.target mysql.service
Wants=mysql.service

[Service]
User=www-data
Group=www-data
WorkingDirectory=/opt/myapp
ExecStart=/usr/bin/node /opt/myapp/server.js
ExecStartPre=/bin/mkdir -p /var/log/myapp
Restart=on-failure
RestartSec=5
Environment=NODE_ENV=production
Environment=PORT=3000
EnvironmentFile=/etc/myapp/env
LimitNOFILE=65536
NoNewPrivileges=true
ProtectSystem=strict
ProtectHome=true

[Install]
WantedBy=multi-user.target
```

```bash
# 部署流程
sudo vim /etc/systemd/system/myapp.service
sudo systemctl daemon-reload       # 必须！
sudo systemctl start myapp
systemctl status myapp
sudo systemctl enable myapp
journalctl -u myapp -f
```

## 11.6 编写自定义 Timer Unit

```ini
# /etc/systemd/system/backup.service
[Unit]
Description=Database Backup Service
[Service]
Type=oneshot
ExecStart=/opt/scripts/backup.sh
User=backup
```

```ini
# /etc/systemd/system/backup.timer
[Unit]
Description=Run Database Backup Daily
[Timer]
OnCalendar=*-*-* 02:00:00
Persistent=true
RandomizedDelaySec=300
Unit=backup.service
[Install]
WantedBy=timers.target
```

```bash
# 部署
sudo systemctl daemon-reload
sudo systemctl enable --now backup.timer
systemctl list-timers

# 验证日历表达式
systemd-analyze calendar "Mon *-*-* 09:00:00"
```

### 常用 OnCalendar 表达式

| 表达式 | 含义 |
| -------- | ------ |
| `*-*-* 02:00:00` | 每天凌晨 2:00 |
| `Mon *-*-* 09:00:00` | 每周一 9:00 |
| `*-*-1 03:00:00` | 每月 1 号 3:00 |
| `*-*-* *:30:00` | 每小时的第 30 分钟 |
| `hourly` / `daily` / `weekly` / `monthly` | 快捷方式 |

## 11.7 Target 与运行级别

| Target | 对应运行级别 | 含义 |
| -------- | ------------- | ------ |
| `poweroff.target` | 0 | 关机 |
| `rescue.target` | 1 | 单用户/救援模式 |
| `multi-user.target` | 3 | 多用户命令行模式 |
| `graphical.target` | 5 | 图形界面模式 |
| `reboot.target` | 6 | 重启 |

```bash
# 查看/切换 target
systemctl get-default
sudo systemctl set-default multi-user.target
sudo systemctl isolate multi-user.target    # 立即切换（不重启）
```

## 11.8 systemd-analyze——启动性能分析

```bash
# 查看系统启动时间
systemd-analyze

# 查看各服务的启动时间
systemd-analyze blame

# 生成 SVG 时间线图表
systemd-analyze plot > boot-timeline.svg

# 查看启动关键路径
systemd-analyze critical-chain

# 验证 unit 文件语法
systemd-analyze verify /etc/systemd/system/myapp.service
```

## 11.9 实战：将 Node.js 应用部署为 Systemd 服务

```bash
# 1. 准备应用
sudo mkdir -p /opt/myapp
sudo cp -r ~/myapp/* /opt/myapp/
cd /opt/myapp && npm install --production

# 2. 创建应用专用用户
sudo useradd -r -s /usr/sbin/nologin -d /opt/myapp myapp
sudo chown -R myapp:myapp /opt/myapp

# 3. 创建 unit 文件（内容见 11.5 节）
sudo tee /etc/systemd/system/myapp.service <<'EOF'
[Unit]
Description=My Express.js Application
After=network.target

[Service]
Type=simple
User=myapp
WorkingDirectory=/opt/myapp
EnvironmentFile=/opt/myapp/.env
ExecStart=/usr/bin/node server.js
Restart=on-failure
RestartSec=5
NoNewPrivileges=true

[Install]
WantedBy=multi-user.target
EOF

# 4. 部署
sudo systemctl daemon-reload
sudo systemctl enable --now myapp

# 5. 验证
systemctl status myapp
journalctl -u myapp -f

# 6. 测试自动重启
sudo kill -9 $(systemctl show -p MainPID myapp --value)
sleep 6
systemctl is-active myapp
# active ← 自动重启成功！
```

## 11.10 本章小结

记住：

- **daemon-reload** 是修改 unit 文件后的必做步骤
- **journalctl -f** 是调试服务的好朋友
- **不要用 root 运行服务**——创建专用用户
- **Restart=on-failure** 让服务具备自愈能力

## 11.11 命令速查表

| 命令 | 用途 | 常用示例 |
| ------ | ------ | ---------- |
| `systemctl start` | 启动服务 | `sudo systemctl start nginx` |
| `systemctl stop` | 停止服务 | `sudo systemctl stop nginx` |
| `systemctl restart` | 重启服务 | `sudo systemctl restart nginx` |
| `systemctl reload` | 重载配置 | `sudo systemctl reload nginx` |
| `systemctl status` | 查看状态 | `systemctl status nginx` |
| `systemctl enable` | 开机自启 | `sudo systemctl enable nginx` |
| `systemctl daemon-reload` | 重载 unit 文件 | `sudo systemctl daemon-reload` |
| `systemctl list-units` | 列出 unit | `systemctl list-units --failed` |
| `journalctl` | 查看日志 | `journalctl -u nginx -f` |
| `systemd-analyze` | 启动分析 | `systemd-analyze blame` |

---
